# Each top-level key is a DSP name.
# To add a DSP, add a new block with its config.
simulator:
  enabled: true
  bidProbability: 0.75
  fixedPrice: 100
  currency: "RON"
  admTemplate: "<vast/>"
  responseDelayMs: 0
